# Telegram
httpx>=0.27.0
google-genai>=0.5.0

# Configuration
pydantic-settings>=2.3.0
pydantic>=2.7.0
python-dotenv>=1.0.0

# MLX inference removed in favour of Gemini API

# Embeddings
sentence-transformers>=3.0.0

# Vector store
qdrant-client>=1.9.0

# Task queue
celery>=5.4.0
redis>=5.0.0

# File watching (hot-reload tools)
watchfiles>=0.24.0

# Video frame extraction
opencv-python-headless>=4.10.0

# OpenAI (Codex task routing)
openai>=1.35.0


# Async MySQL driver (emotion engine)
aiomysql>=0.2.0

# (Acoustic SER removed — emotion detected from transcript text by the brain LLM)

# Skill system — content fetching and extraction
pymupdf>=1.24.0          # PDF → Markdown conversion (read_pdf tool)
trafilatura>=1.12.0      # Webpage main-content extraction (fetch_webpage tool)
markdownify>=0.13.0      # HTML → Markdown fallback (fetch_webpage tool)
DrissionPage>=4.1.0      # Chromium automation for Rednote (social_rednote tool)

# Utilities
numpy>=1.26.0
aiofiles>=24.1.0
